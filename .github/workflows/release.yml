name: Build & Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          java-version: '11'
          distribution: 'temurin'
      - name: Build JAR
        run: mvn package -q
      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: target/ForgeStore-Minecraft-*.jar
          generate_release_notes: true
